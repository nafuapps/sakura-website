---
interface Props {
  side: "left" | "right"
}
const { side = "left" } = Astro.props
---

<div
  class={`flex w-full flex-row justify-end ${side == "left" ? "md:flex-row-reverse" : ""}`}
>
  <slot />
</div>
